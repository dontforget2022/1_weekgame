[gd_scene load_steps=4 format=3 uid="uid://c4hf5wltosrt4"]

[ext_resource type="Texture2D" uid="uid://bhj81mufa3yyr" path="res://sprites/meteors/spr_meteor_3.png" id="1_xa8ev"]

[sub_resource type="GDScript" id="GDScript_e17l3"]
script/source = "extends Area2D
var speed: int
var rotation_speed: int
var direction_x: float
func _ready():
	
	# random number
	#RandomNumberGenerator provides methods for generating pseudo-random numbers
	var rng := RandomNumberGenerator.new()
	#getting room's width and height
	var width = 320
	var height = 240
	#generating random number
	var random_x = rng.randi_range(0,width)
	#not using full value because it will look weird
	var random_y = rng.randi_range(0,10)
	#assigning position
	position = Vector2(random_x,random_y);
	#speed/direction
	speed = rng.randi_range(50, 150)
	direction_x = rng.randf_range(-1,1)
	rotation_speed = rng.randi_range(40, 100)
	
func _process(delta):
	#basic movement
	position += Vector2(direction_x, 1.0) * speed * delta 
	#rotation
	rotation_degrees += rotation_speed * delta
	
func _on_body_entered(body):
	pass
"

[sub_resource type="CircleShape2D" id="CircleShape2D_10yv1"]
radius = 7.0

[node name="meteor_sml" type="Area2D"]
script = SubResource("GDScript_e17l3")

[node name="spr_meteor_sml" type="Sprite2D" parent="."]
texture = ExtResource("1_xa8ev")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("CircleShape2D_10yv1")
